// Get the navbar element by its ID
const navbar = document.getElementById('navbar');

// Function to toggle the 'hide-menu' class on the navbar element
function toggleBtn() {
    navbar.classList.toggle('hide-menu');
}